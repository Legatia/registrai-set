# Path to the SQLite database written by the relayer
DB_FILE_PATH=../relayer/kya.db

# API server port
PORT=3001

# Comma-separated allowed CORS origins (defaults to http://localhost:3000)
CORS_ORIGINS=http://localhost:3000

# API key for write endpoints (POST, PATCH). Leave empty to disable auth.
API_KEY=
